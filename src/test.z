let func = (a,b,c,d) -> a(b+c/d)
var res = ${func(a->2*a,1,200,10)}
var obj = #{
        msg = "I'm hash!"
}
obj.callme = (self,name)-> {
        showln! ""
        input("Hello,"+name,"! " + self.msg)
}
obj.callme(input("What's your name? "))

export res
run!